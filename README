This is a sample application used to showcase some basic funcitonality of the JustWare API.  The API communication code is in the APICommunicator class.

The premise of the application is to perform a search for a name using the Last Name, and to insert new names into JustWare using the API.